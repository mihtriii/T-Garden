// import AsyncStorage from '@react-native-async-storage/async-storage';

// export const getLanguageFromStorage = async () => {
//   try {
//     const language = await AsyncStorage.getItem('lng');
//     return language || 'vi'; // Trả về ngôn ngữ mặc định nếu không tìm thấy trong AsyncStorage
//   } catch (error) {
//     console.error('Error reading language from AsyncStorage:', error);
//     return 'vi'; // Trả về ngôn ngữ mặc định nếu có lỗi xảy ra
//   }
// };